
// import { createContext } from "react";


// export const AuthContext = createContext<[String, Function] | []>([]);

// export const AuthContext = createContext<[String, Function]>(["", () => {}]); // create context with default value
